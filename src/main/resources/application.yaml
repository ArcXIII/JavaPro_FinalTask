spring:
  application:
    name: JavaPro_Final
  jpa:
    show-sql: true
    open-in-view: false
  liquibase:
    change-log: classpath:/db/changelog.xml
  datasource:
    url: jdbc:h2:.\limits;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
    password:
server:
  servlet:
    context-path: /api/v1

app:
  default-limit: 10000
  keep-transaction-history: 3
